## 2026-06-26 코틀린 기초 문법 학습

**학습 내용:**
- 변수 선언(var, val)과 엄격한 데이터 타입 지정을 학습하였습니다. Double, Float, Int, Long, Short 등의 타입별 특성을 이해하고 타입 변환 메서드(toInt() 등)를 습득했습니다.
- 함수 정의 및 기본값 설정, 반환 타입 지정을 학습하였습니다. 특히 Any 타입으로 다양한 데이터를 처리하는 방법을 실습했습니다.
- if 제어문의 조건식 사용 및 값 반환을 학습하였고, 삼항 연산자처럼 활용할 수 있음을 확인했습니다.
- when 제어문을 학습하였습니다. 범위 지정(in 1..10), 데이터 타입 확인(is String), 다중 조건 지정 등 switch보다 강력한 기능을 습득했습니다.
- 반복문(for)을 활용한 수열 계산(라이프니츠 공식으로 원주율 계산)을 학습하였고, 100회, 1000회, 5000회 반복으로 π 값 근사에 성공했습니다.
